name: HackControl
version: ${project.version}
main: kheeto.hackcontrol.HackControl
commands:
  control:
    usage: /<command>
    description: Sends the moderator and the player to the Hack Control server.
permissions:
  hackcontrol.control:
    description: Permission required to use the /contorl command
